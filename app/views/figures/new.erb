<h1>Figures New</h1>

<form action="/figures" method="post">
  <label for="figure_name">Name:</label>
  <input id="figure_name" type="text" name="figure[name]">
  <br></br>

  Figure's Landmarks:
  <br></br>
  <% Landmark.all.each do |landmark| %>
    <input type="checkbox" name="figure[landmark_ids][]" value="landmark_<%= landmark.id %>" id="landmark_<%= landmark.id %>"><%= landmark.name %><br>
  <% end %>

  <br>

  <p>or create a new Landmark:</p>
  <br>
  <h4>Landmark Name: <input type="text" name="landmark[name]" id="new_landmark"></h4>
  <br>

  Figure's Titles:
  <br></br>
  <% Title.all.each do |title| %>
    <input type="checkbox" name="figure[title_ids][]" value="title_<%= title.id %>" id="title_<%= title.id %>"><%= title.name %><br>
  <% end %>

<br></br>

  <p>or create a new Title:</p>
  <br>
  <h4>Title Name: <input type="text" name="title[name]" id="new_title"></h4>
  <br>

  <input type="submit" value="Create New Figure">
</form>
